export default function Returns() {
  return (
    <div style={{maxWidth:'800px',margin:'0 auto',padding:'80px 40px'}}>
      <h1 style={{fontFamily:"'Playfair Display',serif",fontSize:'40px',fontWeight:700,color:'var(--fog)',marginBottom:'32px'}}>Returns Policy</h1>
      <p style={{color:'var(--muted)',lineHeight:1.8,fontSize:'14px'}}>7-day hassle-free returns. No questions asked.</p>
    </div>
  )
}
